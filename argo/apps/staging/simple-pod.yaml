apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: currentai-simple-pod-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/currentai-org/infra'
    targetRevision: dev
    path: charts/simple-pod
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: experiment-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
